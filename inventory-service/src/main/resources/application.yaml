server:
  port: 8090

spring:
  application:
    name: inventory-service
    jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

grpc:
  server:
    port: 4050
    
limit:
  lowStockThresold: 10
