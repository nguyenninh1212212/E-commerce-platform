spring:
  config:
    activate:
      on-profile: docker
  data:
    mongodb:
      uri: mongodb://developer:password123@mongodb:27017/variantdb?authSource=admin

  kafka:
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.ByteArraySerializer
    bootstrap-servers:
      - kafka:9092
